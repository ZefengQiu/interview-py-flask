class State:

	# not use this class for not, key value pair will be enough
	# but if state model is more complex
	# will have to add property and change key value pair to "id": "State" pair

	def __init__(self, id, value):
		self.id = id
		self.value = value
